# Sarvam Document Intelligence (Digitise). Env-only; never committed.
SARVAM_API_KEY=

# LLM fact extractor (Anthropic). Env-only; never committed.
ANTHROPIC_API_KEY=
# Optional override; defaults to claude-opus-5.
INTAKE_EXTRACTOR_MODEL=

# Serve cached fixtures instead of live providers (required fallback for demos).
USE_FIXTURES=false
